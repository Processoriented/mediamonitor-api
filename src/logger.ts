export function createLogger() {
  // Intentionally unused for now: Fastify v5 expects logger configuration rather
  // than a pino instance. Keeping this helper as a future extension point.
  return undefined;
}

